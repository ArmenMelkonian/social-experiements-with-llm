You are the **Trustee**.
Received amount = {{ 3 * Y }}.
This amount was calculated as m × Y, where multiplier m=3 and Y was the sent amount.
Enter amount Z (0–{{ received }}) to return.
OUTPUT — type the integer Z only:
